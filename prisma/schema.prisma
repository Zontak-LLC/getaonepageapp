generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  name         String
  passwordHash String   @map("password_hash")
  role         String   @default("user") // "user" | "admin"
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  @@map("users")
}

model CreditRecord {
  id        String   @id @default(cuid())
  email     String   @unique
  total     Int      @default(3)
  used      Int      @default(0)
  plan      String   @default("standard")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("credit_records")
}

model ExecutionSession {
  id        String   @id @default(cuid())
  sessionId String   @unique @map("session_id")
  userEmail String   @default("") @map("user_email")
  data      Json
  expiresAt DateTime @map("expires_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("execution_sessions")
}

model ConversationSession {
  id            String   @id @default(cuid())
  sessionId     String   @unique @map("session_id")
  userEmail     String   @map("user_email")
  messages      Json     // ChatMessage[]
  partialSpec   Json?    @map("partial_spec")
  partialIntake Json?    @map("partial_intake")
  specStatus    String   @default("gathering") @map("spec_status")
  buildResult   Json?    @map("build_result")
  expiresAt     DateTime @map("expires_at")
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")

  @@map("conversation_sessions")
}
